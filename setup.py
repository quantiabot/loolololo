from setuptools import setup, find_packages

setup(
    name="sarncss",
    version="1.00",
    description="Tiny package to generate random letters and numbers",
    author="sorncss",
    packages=find_packages(),
    python_requires=">=3.8",
)